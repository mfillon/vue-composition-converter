<template>
  <div class="las-data">
    <las-header v-bind="header"/>
    <las-dummy
      v-if="!content.items.length"
      v-bind="dummy"
      class="las-data__dummy"
    />
    <las-data-items
      v-else
      v-bind="content"
    />
  </div>
</template>

<script lang="ts">
import { Component, Prop, Vue } from 'vue-property-decorator';

import type {
  ILasHeaderData,
  ILasDataItems,
  ILasDummy,
} from './types';

@Component({
  components: {
    lasDummy: () => import('./lasDummy.vue'),
    lasHeader: () => import('./lasHeader.vue'),
    lasDataItems: () => import('./lasDataItems.vue'),
  },
})
export default class LasData extends Vue {
  @Prop({ type: Object, required: true })
  readonly header!: ILasHeaderData;
  @Prop({ type: Object, required: true })
  readonly content!: ILasDataItems;
  @Prop({ type: Object, required: true })
  readonly dummy!: ILasDummy;
}
</script>

<style lang="scss" scoped>
.las-data {
  position: relative;

  &__dummy {
    position: absolute;
    top: 0;
    right: 20%;

    @include media-down(md) {
      right: -26px;
    }

    @include media-down(sm) {
      position: static;
    }
  }
}
</style>
